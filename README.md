# Vert.X configurer sample

## Sample of Vertx configurer project

Configure and deploy multiple verticles, made with Normal and Reactive Verticles

Uses vertx-config, RxJava2 (optional)

To run use:
```
mvn compile vertx:run
```

To build use:
```
mvn clean package
```